# Absolute origin of the deployed site, no trailing slash.
# Used for canonical links, Open Graph URLs, sitemap.xml and robots.txt.
# Must be set at BUILD time — the site is statically prerendered.
NEXT_PUBLIC_SITE_URL=https://example.com
